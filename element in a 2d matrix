// Problem -- 3
// Search an element in a 2D matrix 

int main() {
    int arr[2][2] = { {10,20},
                    {30,40} };
                    
    int searchElement;
    cin >> searchElement;
    
       for(int i = 0; i < 2; i++) {
        for(int j = 0; j < 2; j++) {
            if(arr[i][j] == searchElement){
                cout << i << " " << j;
                return 0;
                }
            }
       }
    cout << "element not found..";
    
}
